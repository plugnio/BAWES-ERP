import { roles } from '../production/rbac';

// No development-specific categories or roles needed
export { roles };
export const categories = []; // Empty array since permissions are auto-discovered 